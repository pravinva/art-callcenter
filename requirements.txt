# Core Databricks SDK
databricks-sdk>=0.40.0
databricks-sql-connector>=3.0.0
databricks-vectorsearch>=0.60.0  # Vector Search client for local testing

# Zerobus SDK - install from GitHub
# pip install git+https://github.com/databricks/zerobus-sdk-py.git
# Note: Package name is databricks-zerobus-ingest-sdk

# GenAI and ML
mlflow>=2.10.0
# databricks-agents - install separately if available

# Data processing
pandas>=2.0.0
faker>=20.0.0

# Web framework
streamlit>=1.28.0

# Async support
aiohttp>=3.9.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0

